# Initialise built-in Shiny application

Initialise built-in Shiny application

## Usage

``` r
ww_run_app(package = "wowi")
```

## Arguments

- package:

  package name ("wowi").

## Examples

``` r
if(interactive()) ww_run_app()
```
